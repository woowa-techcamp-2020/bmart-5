import React, { ReactNode } from 'react';
import MainPage from './pages/MainPage';

const App:Function = ():ReactNode => {
  return (
    <div className="App">
      <MainPage/>
    </div>
  );
}

export default App;
